<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.spector.template">

    <uses-permission android:name="android.permission.INTERNET"/>   <!-- for api/http -->

    <!-- android:usesCleartextTraffic="true" - since we are not using https in the api -->
    <!-- android:name=".app.App" - for DI (hilt) -->
    <application
        android:name="com.spector.template.app.App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/appName"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Template"
        android:usesCleartextTraffic="true"
        tools:targetApi="34">
        <activity
            android:name="com.spector.template.presentation.main.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Template">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>